name: my_bbq
description: Monitors and controls the Heater Meter BBQ controller

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  collection: ^1.15.0
  syncfusion_flutter_charts: ^19.1.69
  provider: ^5.0.0
  overflow_view: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
